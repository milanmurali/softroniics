#include <stdio.h>
#include <string.h>

int main()
{
    int arr[5];
    int i, j = 0, k;
    printf("Enter the 5 Elements of Array : \n ");

    for (i = 0; i < 5; i++)
    {
        scanf("%d", &arr[i]);
    }
        return 0;
}